{{- if .Values.networkPolicies.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-istio-and-oauth2-proxy
  namespace: {{ include "dex.namespace" . }}
spec:
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: {{ include "dex.istioNamespace" . }}
      podSelector:
        matchLabels:
          app: istio-ingressgateway
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: {{ include "dex.istioNamespace" . }}
      podSelector:
        matchLabels:
          app: istiod
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: {{ include "dex.oauth2ProxyNamespace" . }}
      podSelector:
        matchLabels:
          app.kubernetes.io/name: oauth2-proxy
  podSelector:
    matchLabels:
      app: dex
  policyTypes:
  - Ingress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-allow-same-namespace-auth
  namespace: {{ include "dex.namespace" . }}
spec:
  ingress:
  - from:
    - podSelector: {}
  podSelector: {}
  policyTypes:
  - Ingress
{{- end }}
