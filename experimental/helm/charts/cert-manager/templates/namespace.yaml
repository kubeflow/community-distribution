{{- $certManagerNamespace := (index .Values "cert-manager" "namespace") | default "cert-manager" -}}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ $certManagerNamespace | quote }}
  labels:
    pod-security.kubernetes.io/enforce: restricted
