{{- if ne .Values.scenario "platform" -}}
{{- fail "only the platform scenario is supported" -}}
{{- end -}}
{{- if ne .Release.Namespace "kubeflow-user-example-com" -}}
{{- fail "this chart must be installed in kubeflow-user-example-com" -}}
{{- end -}}
{{- if or (not (kindIs "string" .Values.database.password)) (empty .Values.database.password) (eq .Values.database.password "REPLACE_ME_DATABASE_PASSWORD") -}}
{{- fail "database.password must be supplied in a private values file" -}}
{{- end -}}
